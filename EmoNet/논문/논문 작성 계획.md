먼저 대략적인 목차를 작성해야 한다

모든 내용은 전체 설계를 하나의 파이프 라인으로 두고 각 요소에 대하여 에피소드 형식으로 적어야 한다. 아이디어와 가설 그리고 실험과 결론으로 나누어서 각 에피소드를 작성한다. 
적어야할 에피소드는 다음과 같을것 같다
1. Encoder_ML test
2. Encoder_LLM test
3. 뉴런의 기능 넣었다 뺐다
4. z의 크기 변형 실험
5. 나머지는 아이디어니깐 안해도 될듯
6. trait을 N안에다가 넣어버리는 설계로 바꿔야 겠다. 그렇다면 N이 바뀌어서는 안된다. 아니다 된다. 기억의 왜곡으로 설명하면 된다.(처음엔 이렇게 기억 되었는데 새로운 경험을 하며 그것이 바뀌어 졌다.)

용어 정리
1. x: 입력
2. Encoder: 초기 자극 변환기
3. stimvec: 자극 벡터
4. N: 신경망
5. $H_{branch}$: 감정 브랜치 히스토리
6. Decoder: 감정 요약 벡터 변환기
7. z: 감정 요약 벡터: H_branch의 요약된 벡터 64, 128, 256
8. 



N에 대한 대략적인 설명은 다음과 같다.
기본적으로 비선형 연산을 하는 뉴런들의 집합이다. 
<뉴런>
- 입력: stimvec
- 


<목차>
- 초록
- 선행연구
- 전체 설계  $x \rightarrow Encoder(x) \rightarrow stim vec \rightarrow \mathcal{N}(\mathcal{C}) \rightarrow H_{branch} \rightarrow E_H \rightarrow z \rightarrow R_{tone}(z) \rightarrow s \rightarrow G_{prompt} \rightarrow LLM \rightarrow y$
- 인풋($x$)
- 인코더($Encoder$)
- 
- 신경망($mathcal{N}(\cdot,p,\mathcal{C})$)
- 